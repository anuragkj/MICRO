DATA SEGMENT
ORG 1000H
N DW 5
RES DW ?
DATA ENDS
CODE SEGMENT
ASSUME CS:CODE, DS:DATA
START: MOV AX,DATA
       MOV DS,AX
       MOV AX,N
       MOV CX,AX
       DEC CX
BACK: MUL CX
      DEC CX
      JNZ BACK
      MOV RES,AX
      INT 3
CODE ENDS
END START

